# Boot the environment
require_relative 'config/environment'
Wukong::Deploy.boot!(Wukong::Deploy::Configuration, "<%= @root %>")

# Require library tasks
require 'wukong-deploy/tasks'

# Require RSpec tasks
require 'rspec/core/rake_task'
RSpec::Core::RakeTask.new(:spec)

# Require application tasks
Dir[File.join("<%= @root %>", 'lib/tasks/**/*.rb')].each { |path| require(path) }

task :default => :spec
